import {
    name
} from 'commander';
import {
    v4 as uuidv4
} from 'uuid';


let hobbies = [Cinéma, Musique, Photographie, Série, voyage, cuisine, randonnée, lecture, jeux - vidéo, foot]
let user = [];
for (let i = 0; i < 1000; i++) {
    let id = uuidv4();
    user.push({
        id: id
        hobbies: hobbie[Math.floor(Math.random() * hobbies.length)]
    });
}
console.log(user);